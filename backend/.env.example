# Server Configuration
NODE_ENV=development
PORT=3002
FRONTEND_URL=http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=prix_du_coeur
DB_USER=prix_user
DB_PASSWORD=VotreMotDePasseSecurise

# JWT Authentication
# Générez avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=remplacez_par_une_cle_secrete_longue_et_complexe_minimum_32_caracteres
JWT_EXPIRES_IN=7d

# Mistral AI Configuration
# Obtenez votre clé sur: https://console.mistral.ai/
MISTRAL_API_KEY=votre_cle_api_mistral
MISTRAL_MODEL=mistral-small-latest

# Encryption Key (exactement 64 caractères hex = 32 bytes)
# Générez avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ NE JAMAIS CHANGER APRÈS LE PREMIER CHIFFREMENT !
ENCRYPTION_KEY=remplacez_par_64_caracteres_hex_generes_avec_la_commande_ci_dessus

# Logs Configuration
LOG_LEVEL=info
LOG_RETENTION_DAYS=90

# CORS Configuration
# En développement: http://localhost:5173
# En production: ajoutez votre domaine (ex: https://mondomaine.fr)
CORS_ORIGIN=http://localhost:5173

# Email SMTP Configuration (optionnel mais recommandé)
# Pour Gmail: activer "Mots de passe des applications" dans les paramètres Google
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre.email@gmail.com
SMTP_PASSWORD=votre_mot_de_passe_application
SMTP_FROM=Prix du Coeur <noreply@votredomaine.com>
